```
ALTER TABLE vendors
ADD COLUMN vendor_group INT NOT NULL;

SELECT *
FROM t1
INNER JOIN t2 ON condition;

SELECT *
FROM t
GROUP BY c1, c2...

SELECT *
FROM t
GROUP BY c1
HAVING condition

INSERT INTO
```

